{% extends 'layout.html.twig' %}
{% block body %}
        <div id="global">

            <li>_________________________________</li>
            <li><br></br></li>
            {% for storage in storages %}
                <div class="list-collec">
                    {{storage.room}}
                    {{storage.congelateur}}
                    {{storage.etagere}}
                    {{storage.rack}}
                    {{storage.typeConteneur}}
                    {{storage.positionConteneur}}
                    {{storage.description}}
                    {{storage.comment}}
                </div>
            {% endfor %}
        <br></br>        
        </div>  
{% endblock %}